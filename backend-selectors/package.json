{
  "name": "clearconnect-selectors",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ClearConnect Selectors",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
